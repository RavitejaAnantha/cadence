"""Cadence workflow tools: doc generation, doc-vs-code consistency, intent checks, changelog.

These are local, dependency-light equivalents of skills you would otherwise reach for in a
suite like GStack (/document-release, /review). They run offline and are wired into the
pre-commit hook so the workflow is enforced, not just documented.
"""
